type Notification @key(fields: "id"){
      id: String!
      case: Case!
      text: String!
}

type Case @key(fields: "id") @extends {
    id: String! @external
}

type Query {
    getNotificationById(id: String): Notification
}